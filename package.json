{
  "name": "existdb-openapi",
  "private": true,
  "scripts": {
    "deploy": "xst package install target/existdb-openapi-*.xar",
    "test": "cypress run",
    "test:open": "cypress open"
  },
  "devDependencies": {
    "@existdb/xst": "^4.1.2",
    "cypress": "^16.0.0"
  }
}
