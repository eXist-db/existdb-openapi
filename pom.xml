<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.exist-db</groupId>
        <artifactId>exist-apps-parent</artifactId>
        <version>2.0.0</version>
        <relativePath/>
    </parent>

    <groupId>org.exist-db</groupId>
    <artifactId>existdb-openapi</artifactId>
    <version>0.9.0-SNAPSHOT</version>

    <name>eXist-db OpenAPI</name>
    <description>OpenAPI-described HTTP surface for eXist-db: database management, query execution, language services, user management, package management, search, and cross-app linking</description>
    <url>https://github.com/eXist-db/existdb-openapi</url>

    <organization>
        <name>The eXist-db Authors</name>
        <url>https://exist-db.org</url>
    </organization>

    <licenses>
        <license>
            <name>GNU Lesser General Public License, version 2.1</name>
            <url>https://opensource.org/licenses/LGPL-2.1</url>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/eXist-db/existdb-openapi.git</url>
        <connection>scm:git:https://github.com/eXist-db/existdb-openapi.git</connection>
        <developerConnection>scm:git:https://github.com/eXist-db/existdb-openapi.git</developerConnection>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.source>21</project.build.source>
        <project.build.target>21</project.build.target>

        <exist.version>7.0.0-SNAPSHOT</exist.version>

        <!-- Node toolchain for Cypress integration tests (frontend-maven-plugin).
             Node 24 is current LTS. -->
        <node.version>v24.11.1</node.version>
        <npm.version>11.8.0</npm.version>

        <!-- used in the EXPath Package Descriptor -->
        <package-name>http://exist-db.org/pkg/openapi</package-name>
        <package-abbrev>existdb-openapi</package-abbrev>

        <!-- language services module (diagnostics, completions, hover, etc.) -->
        <langservice.module.namespace>http://exist-db.org/xquery/langservice</langservice.module.namespace>
        <langservice.module.java.classname>org.exist.xquery.modules.openapi.langservice.LangServiceModule</langservice.module.java.classname>

        <!-- cursor module (server-side cursors for paginated query execution) -->
        <cursor.module.namespace>http://exist-db.org/xquery/cursor</cursor.module.namespace>
        <cursor.module.java.classname>org.exist.xquery.modules.openapi.cursor.CursorModule</cursor.module.java.classname>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.exist-db</groupId>
            <artifactId>exist-core</artifactId>
            <version>${exist.version}</version>
            <scope>provided</scope>
            <exclusions>
                <exclusion>
                    <groupId>xalan</groupId>
                    <artifactId>xalan</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>xalan</groupId>
                    <artifactId>serializer</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fifesoft</groupId>
                    <artifactId>rsyntaxtextarea</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Transitively supplied by exist-core, but declared explicitly because
             our code references types from these libs directly (required by
             maven-dependency-plugin's analyze rule inherited from exist-apps-parent). -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>2.26.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>3.2.4</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>antlr</groupId>
            <artifactId>antlr</artifactId>
            <version>2.7.7</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <version>3.0.2</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.lacuna</groupId>
            <artifactId>bifurcan</artifactId>
            <version>0.2.0-rc1</version>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <profiles>
        <!-- Integration tests require exist-core installed locally (mvn install from exist repo).
             Activate with: mvn test -Pintegration-tests -->
        <profile>
            <id>integration-tests</id>
            <dependencies>
                <dependency>
                    <groupId>org.exist-db</groupId>
                    <artifactId>exist-core</artifactId>
                    <version>${exist.version}</version>
                    <type>test-jar</type>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>org.exist-db</groupId>
                    <artifactId>exist-start</artifactId>
                    <version>${exist.version}</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                    <version>4.13.2</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>

        <!-- End-to-end Cypress tests via frontend-maven-plugin.
             Requires a running eXist instance on http://localhost:8080
             with the built XAR deployed (see Step "Run e2e tests" in README).
             Activate with: mvn verify -Pe2e-tests -->
        <profile>
            <id>e2e-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>2.0.0</version>
                        <configuration>
                            <nodeVersion>${node.version}</nodeVersion>
                            <npmVersion>${npm.version}</npmVersion>
                        </configuration>
                        <executions>
                            <execution>
                                <id>install node and npm</id>
                                <goals>
                                    <goal>install-node-and-npm</goal>
                                </goals>
                                <phase>pre-integration-test</phase>
                            </execution>
                            <execution>
                                <id>npm ci</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>pre-integration-test</phase>
                                <configuration>
                                    <!-- ignore-scripts skips Cypress's postinstall binary
                                         fetch; we install it explicitly below for cacheability. -->
                                    <arguments>ci --ignore-scripts</arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>cypress install</id>
                                <goals>
                                    <goal>npx</goal>
                                </goals>
                                <phase>pre-integration-test</phase>
                                <configuration>
                                    <arguments>cypress install</arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>cypress run</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>integration-test</phase>
                                <configuration>
                                    <arguments>test</arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>ro.kuberam.maven.plugins</groupId>
                <artifactId>kuberam-expath-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>exist-db</id>
            <name>eXist-db Releases</name>
            <url>https://repo.exist-db.org/repository/exist-db/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>exist-db-snapshots</id>
            <name>eXist-db Snapshots</name>
            <url>https://repo.exist-db.org/repository/exist-db-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
</project>
